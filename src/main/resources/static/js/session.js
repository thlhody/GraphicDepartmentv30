document.addEventListener('DOMContentLoaded', function() {
    // Create a new Bootstrap modal instance
    var resumeModal = new bootstrap.Modal(document.getElementById('resumeConfirmationModal'));
    // Show the modal
    resumeModal.show();
});